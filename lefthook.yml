pre-commit:
  parallel: true
  commands:
    check:
      glob: "*.{ts,tsx,json,css,html}"
      run: pnpm exec biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true
    typecheck:
      glob: "*.{ts,tsx}"
      run: pnpm typecheck
